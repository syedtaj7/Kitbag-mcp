# image.exif_metadata

Extract EXIF metadata tags (e.g. camera, GPS, capture date) from JPEG images.

## Usage

Provide the image path/data.

### Input Schema

- `filePath` (string, optional): Local path to the image file.
- `fileData` (string, optional): Base64 encoded image data.
