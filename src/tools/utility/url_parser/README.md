# utility.url_parser

Parse a URL string into its component parts (protocol, hostname, port, pathname, search params, hash).

## Usage

Provide the `url` string to parse.

### Input Schema

- `url` (string, required): The URL to parse.
