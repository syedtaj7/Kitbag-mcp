# utility.ssl_check

Retrieve SSL certificate details (expiration, issuer, subject) for a given domain/host.

## Usage

Provide the `host` and optional `port`.

### Input Schema

- `host` (string, required): Domain name or host address.
- `port` (number, optional): Connection port. Defaults to 443.
