# utility.zip_create

Compress files or folders into a single ZIP archive.

## Usage

Provide the array of `files` and the target `outputPath`.

### Input Schema

- `files` (array, required): Array of files to include, specifying name and either inline content or filePath.
- `outputPath` (string, required): Output file path.
