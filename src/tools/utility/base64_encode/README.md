# utility.base64_encode

Encode a text string or a local file to Base64 format.

## Usage

Provide either inline `text` or a `filePath`.

### Input Schema

- `text` (string, optional): Plain text.
- `filePath` (string, optional): Local path to file.
