# utility.random_number

Generate a cryptographically secure random integer within a specified range [min, max].

## Usage

Provide optional `min` and `max` values.

### Input Schema

- `min` (number, optional): The minimum value (inclusive). Defaults to 1.
- `max` (number, optional): The maximum value (inclusive). Defaults to 100.
