# utility.zip_extract

Extract a ZIP archive to a destination folder.

## Usage

Provide the `zipPath` and the `outputPath`.

### Input Schema

- `zipPath` (string, required): ZIP file to extract.
- `outputPath` (string, required): Output folder.
