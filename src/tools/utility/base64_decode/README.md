# utility.base64_decode

Decode a Base64 encoded string back to its original plain text or save to a file.

## Usage

Provide the `base64` string.

### Input Schema

- `base64` (string, required): Base64 format payload.
- `outputPath` (string, optional): Local path to save binary output.
