# developer.json_formatter

Validate, format (pretty-print), or minify JSON strings.

## Usage

Provide the `json` string.

### Input Schema

- `json` (string, required): JSON string.
- `minify` (boolean, optional): If true, minifies. Defaults to false.
- `indent` (number, optional): Indent spacing. Defaults to 2.
