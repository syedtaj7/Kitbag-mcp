# developer.code_detector

Detect the programming language of a raw code snippet using heuristic analyzers.

## Usage

Provide the `code` snippet.

### Input Schema

- `code` (string, required): The raw programming code.
