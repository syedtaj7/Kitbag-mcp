# developer.markdown_lint

Lint Markdown text for common format, structural, and style violations.

## Usage

Provide the `markdown` content.

### Input Schema

- `markdown` (string, required): Markdown text to lint.
