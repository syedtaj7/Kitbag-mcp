# developer.regex_tester

Test a regular expression against a string and return details about matches and capture groups.

## Usage

Provide the `regex` and `text` to test.

### Input Schema

- `regex` (string, required): Pattern.
- `text` (string, required): String to match.
- `flags` (string, optional): Search modifiers. Defaults to 'g'.
