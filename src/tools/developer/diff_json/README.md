# developer.diff_json

Perform a structural, nested properties diff comparison between two JSON objects.

## Usage

Provide the JSON strings of `jsonA` and `jsonB`.

### Input Schema

- `jsonA` (string, required): Baseline JSON object.
- `jsonB` (string, required): Modified JSON object.
