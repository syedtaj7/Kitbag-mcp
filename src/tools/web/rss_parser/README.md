# web.rss_parser

Parse an RSS or Atom feed XML URL and return feed items in a structured JSON array.

## Usage

Provide the feed `url` to parse.

### Input Schema

- `url` (string, required): The URL of the RSS or Atom XML feed.
