# web.to_markdown

Fetch a web page, clean up clutter (scripts, styles, footer, navigation), and convert the main body to Markdown.

## Usage

Provide the `url` of the web page to scrape.

### Input Schema

- `url` (string, required): The URL of the webpage to scrape and convert.
