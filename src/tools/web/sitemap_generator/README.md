# web.sitemap_generator

Fetch and parse a sitemap.xml URL, extracting all listed URLs.

## Usage

Provide the sitemap URL (or base domain).

### Input Schema

- `url` (string, required): The sitemap XML URL, or homepage URL.
