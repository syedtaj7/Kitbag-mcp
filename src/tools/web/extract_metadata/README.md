# web.extract_metadata

Extract metadata, OpenGraph tags, Twitter cards, and JSON-LD structured data from a web page.

## Usage

Provide the `url` of the web page to inspect.

### Input Schema

- `url` (string, required): The URL of the webpage to scrape metadata from.
