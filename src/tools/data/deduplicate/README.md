# data.deduplicate

Remove duplicate entries from a JSON array of objects or strings.

## Usage

Provide the array in JSON string format.

### Input Schema

- `arrayData` (string, required): JSON string array.
- `key` (string, optional): Unique key to deduplicate objects by.
