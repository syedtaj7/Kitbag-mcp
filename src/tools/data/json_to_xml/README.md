# data.json_to_xml

Convert a JSON object to an XML text string.

## Usage

Provide the JSON string of the object.

### Input Schema

- `jsonData` (string, required): JSON string of the object.
- `rootName` (string, optional): Root element tag name. Defaults to 'root'.
