# data.json_to_yaml

Convert a JSON object or string to a YAML text string.

## Usage

Provide the JSON string of the object.

### Input Schema

- `jsonData` (string, required): JSON string of the object to convert.
