# pdf.extract_tables

Extract tabular structures from a PDF text stream and format them in Markdown.

## Usage

Provide either `filePath` or `fileData` (base64 string).

### Input Schema

- `filePath` (string, optional): Local filesystem path to the PDF file.
- `fileData` (string, optional): Base64 encoded PDF file data.
